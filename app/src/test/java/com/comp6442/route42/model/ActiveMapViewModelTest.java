package com.comp6442.route42.model;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import com.comp6442.route42.R;
import com.comp6442.route42.data.ActiveMapViewModel;
import com.comp6442.route42.data.model.Activity;
import com.comp6442.route42.data.model.BaseActivity;
import com.comp6442.route42.utils.MockLocation;

import org.junit.Assert;
import org.junit.Test;
import org.mockito.Mockito;

import java.util.Date;

public class ActiveMapViewModelTest {
    @Test
    public void MapViewTest(){
//        long elapsedTime = 0;
//        Date date1 = new Date(2021,10,19);
//        Date lastupdateDate = new Date(2021,10,18);
//        ActiveMapViewModel activeMapViewModel1 = new ActiveMapViewModel();
//        ActiveMapViewModel activeMapViewModel = mock(ActiveMapViewModel.class);
//        Mockito.doThrow(new Exception()).when().getDeclaredConstructors()
//        Assert.assertTrue(activeMapViewModel!=null);
//        activeMapViewModel1.setActivityType(Activity.Activity_Type.RUNNING);
//
//        Assert.assertTrue(activeMapViewModel1.getActivityType().equals(Activity.Activity_Type.RUNNING));
//        when(activeMapViewModel.getActivityType()).thenReturn(Activity.Activity_Type.CYCLING);
//
//        Assert.assertTrue(activeMapViewModel.getActivityType().equals(Activity.Activity_Type.CYCLING));
////        activeMapViewModel.setLastUpdateTime(lastupdateDate);
//        when(activeMapViewModel.getLastUpdateTime()).thenReturn(lastupdateDate);
//        Assert.assertTrue(activeMapViewModel.getLastUpdateTime()== lastupdateDate);
////        activeMapViewModel.updateElapsedTime();
//        when(activeMapViewModel.getElapsedTime()).thenReturn(30L);
//        Assert.assertTrue(activeMapViewModel.getElapsedTime()==30L);
////        activeMapViewModel.updateElapsedTime();
//
//        Assert.assertEquals(activeMapViewModel.getElapsedTime()+ BaseActivity.getElapsedTimeSeconds(date1,lastupdateDate),86430);
//        Assert.assertEquals(elapsedTime+ BaseActivity.getElapsedTimeSeconds(date1,lastupdateDate),86400);
//
//        activeMapViewModel.reset();
//        Assert.assertEquals(elapsedTime,0);
//        Assert.assertEquals(activeMapViewModel.getActivityData(),null);
//        Assert.assertEquals(activeMapViewModel.getSnapshotFileName(),null);
//        Assert.assertEquals(activeMapViewModel.getLastUpdateTime(),null);
//        Assert.assertEquals(activeMapViewModel.getActivityType(),null);

    }
}
